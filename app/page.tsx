import Layout from '../components/layout/Layout'; // Importing Layout from components folder
import HomeClient from '../components/HomeClient'; // Importing HomeClient component

const Home = () => {
  return (
    <Layout>
      <HomeClient />
    </Layout>
  );
};

export default Home;
